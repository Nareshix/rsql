conn = new()

let object = query!(conn, mapping, sql_statemnet, optional_binding)
for i in object{
    print(i.name, i.id)
}

execute!(conn, sql_statemnet, optional_binding)

maybe a 2 in 1 in future or mayben ot most likely not
**fallback**


