use crate::{connection::Connection, prepare::Statement};


impl Statement {
    fn execute(sql: &str, args:Tuple){
        //TODO, identifies the type of tuple and binds accordinlgy. it hanldes the indexing as well
    }
}
